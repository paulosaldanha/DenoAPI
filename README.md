#Simpre DENO Rest API crud that saves data on postgres.

**to run:**
```
deno run -A index.js
```

**Endpoints:**
* get("/beers")
* get("/beers/:id")
* post("/beers")
* put("/beers/:id")
* delete("/beers/:id"); 
